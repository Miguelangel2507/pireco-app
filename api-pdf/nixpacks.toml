[phases.setup]
nixPkgs = ['python311', 'weasyprint', 'pango', 'cairo', 'gdk-pixbuf', 'libffi', 'fontconfig', 'freetype']

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port $PORT"
