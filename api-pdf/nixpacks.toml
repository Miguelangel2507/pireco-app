[phases.setup]
nixPkgs = ['python311', 'pango', 'cairo', 'gdk-pixbuf', 'libffi', 'fontconfig', 'freetype']

[phases.install]
cmds = ['python -m ensurepip --upgrade', 'python -m pip install -r requirements.txt']

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port $PORT"
